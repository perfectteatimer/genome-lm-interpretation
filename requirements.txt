# Core dependencies. For an editable install with all extras use:
#   pip install -e ".[interpret,data,dev]"

# --- modelling & training ---
torch>=2.0
transformers>=4.40
datasets>=2.18
peft>=0.10
accelerate>=0.29
evaluate>=0.4

# --- scientific stack ---
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
joblib>=1.3
tqdm>=4.65
pyyaml>=6.0

# --- interpretability (XAI) ---
captum>=0.7
transformers-interpret>=0.10

# --- genomic data utilities (Z-DNA sparse tracks) ---
git+https://github.com/Nazar1997/Sparse_vector.git
